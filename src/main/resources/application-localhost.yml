spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jobseeker
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    hibernate.ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

#  security:
#    user:
#      name: user
#      password: user